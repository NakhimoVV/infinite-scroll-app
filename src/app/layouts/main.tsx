import { FC } from 'react'
import MovieList from '../components/MovieList/MovieList'

const Main: FC = () => {
    return (
        <main>
            <MovieList />
        </main>
    )
}

export default Main
